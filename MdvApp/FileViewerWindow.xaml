<ui:FluentWindow x:Class="MdvApp.FileViewerWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="File viewer"
                 Height="600" Width="820"
                 MinHeight="360" MinWidth="520"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="ViewerTitleBar" Title="File viewer" />

        <!-- Search bar -->
        <Grid Grid.Row="1" Margin="12,4,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ui:TextBox Grid.Column="0" x:Name="SearchBox" PlaceholderText="Search"
                        Icon="{ui:SymbolIcon Search24}" KeyDown="OnSearchKeyDown"
                        TextChanged="OnSearchTextChanged" />

            <ui:Button Grid.Column="1" Content="Search" Icon="{ui:SymbolIcon Search24}"
                       Margin="8,0,0,0" Click="OnSearch" />

            <StackPanel Grid.Column="2" x:Name="SearchResults" Orientation="Horizontal"
                        Visibility="Collapsed" VerticalAlignment="Center" Margin="8,0,0,0">
                <ui:TextBlock x:Name="MatchCounter" VerticalAlignment="Center" Margin="0,0,8,0"
                              Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ui:Button x:Name="PrevButton" Icon="{ui:SymbolIcon ChevronUp24}" Margin="0,0,4,0"
                           Click="OnPrevMatch" ToolTip="Previous (Shift+Enter)" />
                <ui:Button x:Name="NextButton" Icon="{ui:SymbolIcon ChevronDown24}"
                           Click="OnNextMatch" ToolTip="Next (Enter)" />
            </StackPanel>
        </Grid>

        <TabControl Grid.Row="2" x:Name="ViewTabs" Margin="12,0,12,12" SelectionChanged="OnTabChanged">
            <TabItem Header="Hex">
                <TextBox x:Name="HexView"
                         IsReadOnly="True"
                         IsInactiveSelectionHighlightEnabled="True"
                         FontFamily="Consolas"
                         FontSize="13"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         BorderThickness="0"
                         Background="Transparent" />
            </TabItem>
            <TabItem Header="Text">
                <TextBox x:Name="TextView"
                         IsReadOnly="True"
                         IsInactiveSelectionHighlightEnabled="True"
                         FontFamily="Consolas"
                         FontSize="13"
                         AcceptsReturn="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         BorderThickness="0"
                         Background="Transparent" />
            </TabItem>
        </TabControl>
    </Grid>
</ui:FluentWindow>
