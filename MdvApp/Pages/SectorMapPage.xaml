<Page x:Class="MdvApp.Pages.SectorMapPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="640" d:DesignWidth="980"
      Title="Sector Map">
    <Grid Margin="24,8,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TextBlock Grid.Row="0" Text="Sector map" FontTypography="Title" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

        <!-- Legend -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,16">
            <Border Width="14" Height="14" CornerRadius="3" Background="#4C9AFF" VerticalAlignment="Center" />
            <ui:TextBlock Text="Map (sector 0)" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="6,0,18,0" VerticalAlignment="Center" />
            <Border Width="14" Height="14" CornerRadius="3" Background="#3A7A3A" VerticalAlignment="Center" />
            <ui:TextBlock Text="Used" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="6,0,18,0" VerticalAlignment="Center" />
            <Border Width="14" Height="14" CornerRadius="3" Background="#3A3A3A" VerticalAlignment="Center" />
            <ui:TextBlock Text="Free" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="6,0,18,0" VerticalAlignment="Center" />
            <Border Width="14" Height="14" CornerRadius="3" Background="#B03A3A" VerticalAlignment="Center" />
            <ui:TextBlock Text="Damaged" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="6,0,18,0" VerticalAlignment="Center" />
            <ui:TextBlock x:Name="HighlightCaption" VerticalAlignment="Center" Visibility="Collapsed"
                          Foreground="#F2E64B" />
            <ui:Button x:Name="ClearHighlightButton" Visibility="Collapsed" Margin="8,0,0,0"
                       Padding="6,2" VerticalAlignment="Center" Click="OnClearHighlight"
                       ToolTip="Clear highlight" Icon="{ui:SymbolIcon Dismiss24}" />
        </StackPanel>

        <ItemsControl Grid.Row="2" x:Name="SectorItems" VerticalAlignment="Top">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="17" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="2" Height="30" CornerRadius="4" Background="{Binding Fill}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderBrush" Value="{Binding BorderBrush}" />
                                <Setter Property="BorderThickness" Value="{Binding BorderThickness}" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="BorderBrush" Value="#FFFFFF" />
                                        <Setter Property="BorderThickness" Value="2" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Border.ToolTip>
                            <ToolTip>
                                <TextBlock Text="{Binding ToolTip}" />
                            </ToolTip>
                        </Border.ToolTip>
                        <ui:TextBlock Text="{Binding Index}" FontTypography="Caption"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Foreground="White" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
