<Page x:Class="MdvApp.Pages.SectorMapPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="640" d:DesignWidth="980"
      Title="Sector Map">
    <Grid Margin="24,8,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TextBlock Grid.Row="0" Text="Sector map" FontTypography="Title" />

        <!-- Legend -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,16">
            <Border Width="14" Height="14" CornerRadius="3" Background="#4C9AFF" VerticalAlignment="Center" />
            <ui:TextBlock Text="Map (sector 0)" Margin="6,0,18,0" VerticalAlignment="Center" />
            <Border Width="14" Height="14" CornerRadius="3" Background="#3A7A3A" VerticalAlignment="Center" />
            <ui:TextBlock Text="Used" Margin="6,0,18,0" VerticalAlignment="Center" />
            <Border Width="14" Height="14" CornerRadius="3" Background="#3A3A3A" VerticalAlignment="Center" />
            <ui:TextBlock Text="Free" Margin="6,0,18,0" VerticalAlignment="Center" />
            <Border Width="14" Height="14" CornerRadius="3" Background="#B03A3A" VerticalAlignment="Center" />
            <ui:TextBlock Text="Damaged" Margin="6,0,18,0" VerticalAlignment="Center" />
        </StackPanel>

        <ItemsControl Grid.Row="2" x:Name="SectorItems" VerticalAlignment="Top">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="17" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="2" Height="30" CornerRadius="4" Background="{Binding Fill}"
                            ToolTip="{Binding Index, StringFormat='Sector {0}'}">
                        <ui:TextBlock Text="{Binding Index}" FontTypography="Caption"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Foreground="#E0E0E0" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Page>
