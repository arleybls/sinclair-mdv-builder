<ui:FluentWindow x:Class="MdvApp.TextPromptWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Rename"
                 Width="440" SizeToContent="Height"
                 ResizeMode="NoResize"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="PromptTitleBar" Title="Rename" />

        <StackPanel Grid.Row="1" Margin="20,8,20,20">
            <ui:TextBlock x:Name="PromptText" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,8" />
            <ui:TextBox x:Name="InputBox" KeyDown="OnInputKeyDown" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <ui:Button Content="OK" Appearance="Primary" Margin="0,0,8,0" Click="OnOk" />
                <ui:Button Content="Cancel" Click="OnCancel" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
