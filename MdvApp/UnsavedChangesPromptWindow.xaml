<ui:FluentWindow x:Class="MdvApp.UnsavedChangesPromptWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Unsaved changes"
                 Width="460" SizeToContent="Height"
                 ResizeMode="NoResize"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Unsaved changes" />

        <StackPanel Grid.Row="1" Margin="20,8,20,20">
            <ui:TextBlock TextWrapping="Wrap" Foreground="White" Margin="0,0,0,20"
                          Text="The current cartridge has unsaved changes. Save them before continuing?" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Save" Appearance="Primary" Margin="0,0,8,0" Click="OnSave" />
                <ui:Button Content="Don't save" Margin="0,0,8,0" Click="OnDiscard" />
                <ui:Button Content="Cancel" Click="OnCancel" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
